# Frontend Environment Variables (Vite)
# Leave blank for single full-stack server deployment, or set if frontend is hosted separately on Vercel/Netlify
VITE_API_URL=
VITE_SOCKET_URL=

# Backend Server Environment Variables
PORT=5000
CLIENT_ORIGIN=*
GEMINI_API_KEY=
